---
name: errand
description: Send work out to a hand. Load before dispatching anything.
user-invocable: true
allowed-tools: Bash, Read, Glob, Grep, Write, Edit
---

<!-- GENERATED by bin/reeve-adapters. Edit skills/errand/errand.md instead. -->

# errand

Send work out. Load this before dispatching anything, every time.

## Before you write a brief

1. **Which holding.** If the repository is not in `manors.md`, load the `survey` skill first. Never
   dispatch into a repository the household has not been introduced to: the whole point of the
   registry is that a hand never edits the wrong project.
2. **Which office.** Decide by what the hand is allowed to touch, not by how the request was
   phrased. "Have a look at the auth bug" is a scout if the liege wants to understand it and an
   artificer if they want it fixed. If that is genuinely unclear, that is worth one question.
3. **Is this one errand or several.** Split when the work spans holdings, or when two parts would
   edit the same files. Two hands in one repository are fine; two hands in one file are a merge
   conflict you chose to create.
4. **Is it specified well enough.** An underspecified brief produces a hand that guesses or stalls.
   If you cannot write the `{SPEC}` seam, you do not yet know what you are asking for, and that is
   a question for the liege rather than something to paper over.

## Dispatching

```sh
bin/reeve-brief <id> <holding> --office <office> [--type feat|fix|docs|chore] [--base main]
# fill {INTENT} and {SPEC} in the brief
bin/reeve-dispatch <id>                 # add --dry-run first if anything feels uncertain
```

Choosing the id: kebab case, descriptive, short. It becomes the branch name, the directory name and
the label, so `auth-refresh` is right and `fix-the-thing` is not.

### The two seams

- **`{INTENT}`**: the liege's own words, quoted, unimproved. A warden later checks the work against
  this, so paraphrasing it corrupts the review. If the liege was vague, quote the vagueness; do not
  invent precision they did not give you. This seam alone stays prose, because it is a quotation.
- **`{SPEC}`**: your instructions. What to change, what to leave alone, how to verify. Name files
  you already know are involved. Say explicitly what is out of scope, because scope creep in a
  parallel fleet becomes someone else's merge conflict. Write it telegraphically, for a hand to
  execute rather than for the liege to read: fragments, lists, paths, no courtesy and no narration.
  The liege never reads it, so every sentence you round out is tokens spent on nobody.

## After dispatching

Tell the liege in one line what went out. Not the brief, not the path, not the command. "A scout is
looking into the print styles" is the whole report.

Then let the sentry do its job. Do not poll, and do not narrate progress.

## Relaunching a dead errand

`bin/reeve-dispatch <id>` again. It reuses the existing brief deliberately, so a relaunch cannot
silently change the job. If the brief itself was wrong, that is a new errand with a new id, and say
so to the liege rather than quietly editing history.

## What not to do

- Do not dispatch on an unverified harness without the liege saying so. Report the refusal instead.
- Do not do the work yourself because it looks small. That is how a manager becomes a slow coder,
  and your context is the household's scarcest resource.
- Do not dispatch two hands onto the same branch. Git refuses it, and that refusal is the guard
  rail, not an obstacle.
- Do not fill `{SPEC}` with the whole conversation. A hand reads one brief, not a transcript.
